{
  "version": "1.0.9",
  "scripts": {
    "bundle:serve": "bundle update && bundle exec jekyll serve -o",
    "gem:build": "rm jekyll-theme-acg-*.gem && gem build jekyll-theme-acg.gemspec",
    "gem:push": "gem push jekyll-theme-acg-*.gem"
  },
  "devDependencies": {
    "tailwindcss": "^2.1.4"
  }
}
